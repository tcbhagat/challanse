name: ChallanSe CI and deployment

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: challanse-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm audit --omit=dev --audit-level=high
      - run: npm run build
      - run: bash scripts/test-edge-integration.sh
      - run: bash scripts/test-production-config.sh
      - name: Run landing Lighthouse gate
        run: |
          python3 -m http.server 4173 --directory dist/landing > /tmp/challanse-http.log 2>&1 &
          for _ in {1..20}; do
            curl --fail --silent http://127.0.0.1:4173/ > /dev/null && break
            sleep 1
          done
          npm run audit

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5
        with:
          distribution: temurin
          java-version: 17
          cache: gradle
      - run: npm ci
      - run: npm run check --workspace @challanse/mobile
      - run: npm test --workspace @challanse/mobile
      - run: npm run build --workspace @challanse/mobile

  enrichment:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/enrichment
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: services/enrichment/requirements-dev.txt
      - run: python -m pip install --requirement requirements-dev.txt
      - run: python -m pytest -q
      - name: Validate PostgreSQL migrations
        run: bash ../../scripts/validate-migrations.sh --check-only

  security:
    # Each security check runs as an independent matrix job. fail-fast: false
    # ensures all checks complete regardless of individual failures, so one
    # tool's failure does not prevent others from reporting their results.
    strategy:
      fail-fast: false
      matrix:
        check: [npm-audit, pip-audit, bandit, secret-scanning, config-check, tfscan]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        if: matrix.check == 'npm-audit'
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        if: matrix.check == 'pip-audit' || matrix.check == 'bandit'
        with:
          python-version: '3.12'
      - run: npm ci
        if: matrix.check == 'npm-audit'
      - run: npm audit --omit=dev --audit-level=high
        if: matrix.check == 'npm-audit'
      - run: python -m pip install --requirement services/enrichment/requirements-security.txt
        if: matrix.check == 'pip-audit' || matrix.check == 'bandit'
      - run: python -m pip_audit --requirement services/enrichment/requirements.txt
        if: matrix.check == 'pip-audit'
      - run: bandit -q -r services/enrichment/app
        if: matrix.check == 'bandit'
      - name: Scan Git history for secrets
        if: matrix.check == 'secret-scanning'
        # gitleaks v8.18.2
        run: docker run --rm -v "$PWD:/repo" zricethezav/gitleaks@sha256:cdbb7c955abce02001a9f6c9f602fb195b7fadc1e812065883f695d1eeaba854 git --config /repo/.gitleaks.toml --no-banner --redact --exit-code 1 /repo
      - run: bash scripts/test-production-config.sh
        if: matrix.check == 'config-check'
      - name: Scan Terraform configuration
        if: matrix.check == 'tfscan'
        run: docker run --rm -v "$PWD:/src" aquasec/trivy@sha256:a8ca29078522f30393bdb34225e4c0994d38f37083be81a42da3a2a7e1488e9e config --skip-version-check --exit-code 1 --severity HIGH,CRITICAL /src/infra

  terraform-plan:
    # Terraform/AWS infrastructure validation — gated behind AWS_DEPLOYMENT_FROZEN.
    # This job validates the existing Terraform modules but does not deploy.
    if: vars.AWS_DEPLOYMENT_FROZEN != 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: 1.9.8
      - run: terraform fmt -check -recursive infra/terraform
      - run: terraform -chdir=infra/terraform/staging init -backend=false
      - run: terraform -chdir=infra/terraform/staging validate
      - run: terraform -chdir=infra/terraform/production init -backend=false
      - run: terraform -chdir=infra/terraform/production validate

  integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17.5
        env:
          POSTGRES_DB: challanse_test
          POSTGRES_USER: challanse
          POSTGRES_PASSWORD: challanse-test-only
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U challanse -d challanse_test"
          --health-interval 5s --health-timeout 5s --health-retries 12
      localstack:
        image: localstack/localstack:4.6.0
        env:
          SERVICES: sqs,kms,textract
        ports: ['4566:4566']
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: services/enrichment/requirements-dev.txt
      - run: python -m pip install --requirement services/enrichment/requirements-dev.txt
      - run: |
          export TENANT_CONTEXT_HMAC_KEY="$(printf '%s' 'challanse-test-tenant-context' | sha256sum | awk '{print $1}')"
          PYTHONPATH=services/enrichment python -m pytest -q services/enrichment/tests -m integration
        env:
          TEST_DATABASE_URL: postgresql://challanse:challanse-test-only@127.0.0.1:5432/challanse_test
          AWS_ENDPOINT_URL: http://127.0.0.1:4566
          AWS_ACCESS_KEY_ID: test
          AWS_SECRET_ACCESS_KEY: test
          AWS_DEFAULT_REGION: ap-south-1

  terraform-check:
    # Terraform formatting, initialization, and validation — always runs
    # independently of the AWS deployment freeze. Does not require AWS
    # credentials and does not run plan or apply.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: 1.9.8
      - run: terraform fmt -check -recursive infra/terraform
      - run: terraform -chdir=infra/terraform/staging init -backend=false
      - run: terraform -chdir=infra/terraform/staging validate
      - run: terraform -chdir=infra/terraform/production init -backend=false
      - run: terraform -chdir=infra/terraform/production validate

  # ─── AWS Enrichment Deployment (Deprecated — migrating to Cloudflare-native) ─
  # This job deploys the Python FastAPI enrichment service to AWS ECS.
  # It is gated by AWS_DEPLOYMENT_FROZEN and will be removed once Phase 1
  # (Cloudflare-Native Backend) is complete.
  deploy-enrichment:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && vars.AWS_DEPLOYMENT_FROZEN != 'true' && vars.PILOT_DEPLOY_ENABLED == 'true' && vars.AWS_ENRICHMENT_BOOTSTRAPPED == 'true'
    needs: [validate, android, enrichment, security, integration, terraform-plan]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: production
    outputs:
      image_uri: ${{ steps.image.outputs.uri }}
      migration: 0006_local_pilot.sql
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: aws-actions/configure-aws-credentials@cabfdba3510de1431bac9dba27511d97497fc100 # v5
        with:
          role-to-assume: ${{ vars.AWS_PRODUCTION_TERRAFORM_ROLE_ARN }}
          aws-region: ap-south-1
      - uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: 1.9.8
      - name: Build and push immutable enrichment image
        id: image
        run: |
          image="${AWS_ACCOUNT_ID}.dkr.ecr.ap-south-1.amazonaws.com/${ECR_REPOSITORY}:${GITHUB_SHA}"
          docker build --pull --tag "$image" services/enrichment
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock aquasec/trivy@sha256:a8ca29078522f30393bdb34225e4c0994d38f37083be81a42da3a2a7e1488e9e image --skip-version-check --scanners vuln --exit-code 1 --ignore-unfixed --severity HIGH,CRITICAL "$image"
          docker push "$image"
          digest="$(aws ecr describe-images --repository-name "$ECR_REPOSITORY" --image-ids imageTag="$GITHUB_SHA" --query 'imageDetails[0].imageDigest' --output text)"
          test "$digest" != "None"
          echo "uri=${AWS_ACCOUNT_ID}.dkr.ecr.ap-south-1.amazonaws.com/${ECR_REPOSITORY}@${digest}" >> "$GITHUB_OUTPUT"
        env:
          AWS_ACCOUNT_ID: ${{ vars.AWS_PRODUCTION_ACCOUNT_ID }}
          ECR_REPOSITORY: ${{ vars.AWS_ECR_REPOSITORY }}
      - name: Initialize production Terraform
        run: >-
          terraform -chdir=infra/terraform/production init
          -backend-config="bucket=${{ vars.AWS_TERRAFORM_STATE_BUCKET }}"
          -backend-config="region=ap-south-1"
          -backend-config="kms_key_id=${{ vars.AWS_TERRAFORM_STATE_KMS_KEY_ARN }}"
          -backend-config="use_lockfile=true"
      - name: Apply infrastructure with services stopped
        run: >-
          terraform -chdir=infra/terraform/production apply -auto-approve
          -var="container_image=${{ steps.image.outputs.uri }}"
          -var="adot_collector_image=${{ vars.AWS_ADOT_COLLECTOR_IMAGE }}"
          -var="cloudflared_image=${{ vars.AWS_CLOUDFLARED_IMAGE }}"
          -var="certificate_arn=${{ vars.AWS_PRIVATE_ALB_CERTIFICATE_ARN }}"
          -var="terraform_state_bucket_arn=arn:aws:s3:::${{ vars.AWS_TERRAFORM_STATE_BUCKET }}"
          -var="expected_aws_account_id=${{ vars.AWS_PRODUCTION_ACCOUNT_ID }}"
          -var="backup_destination_vault_arn=${{ vars.AWS_BACKUP_DESTINATION_VAULT_ARN }}"
          -var="play_integrity_cloud_project_number=${{ vars.PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER }}"
          -var="monthly_budget_usd=${{ vars.AWS_PRODUCTION_MONTHLY_BUDGET_USD }}"
          -var="budget_email=${{ vars.AWS_BUDGET_EMAIL }}"
          -var="secondary_budget_email=${{ vars.AWS_SECONDARY_BUDGET_EMAIL }}"
          -var="github_oidc_provider_arn=${{ vars.AWS_GITHUB_OIDC_PROVIDER_ARN }}"
          -var="services_enabled=false"
      - name: Apply PostgreSQL migrations as one-off task
        run: |
          cluster="$(terraform -chdir=infra/terraform/production output -raw ecs_cluster_name)"
          task_definition="$(terraform -chdir=infra/terraform/production output -raw migration_task_definition_arn)"
          subnets="$(terraform -chdir=infra/terraform/production output -json private_subnet_ids | jq -r 'join(",")')"
          security_group="$(terraform -chdir=infra/terraform/production output -raw service_security_group_id)"
          task_arn="$(aws ecs run-task --cluster "$cluster" --task-definition "$task_definition" --launch-type FARGATE --network-configuration "awsvpcConfiguration={subnets=[$subnets],securityGroups=[$security_group],assignPublicIp=DISABLED}" --query 'tasks[0].taskArn' --output text)"
          test "$task_arn" != "None"
          aws ecs wait tasks-stopped --cluster "$cluster" --tasks "$task_arn"
          exit_code="$(aws ecs describe-tasks --cluster "$cluster" --tasks "$task_arn" --query 'tasks[0].containers[0].exitCode' --output text)"
          test "$exit_code" = "0"
      - name: Enable API and worker after migrations
        run: >-
          terraform -chdir=infra/terraform/production apply -auto-approve
          -var="container_image=${{ steps.image.outputs.uri }}"
          -var="adot_collector_image=${{ vars.AWS_ADOT_COLLECTOR_IMAGE }}"
          -var="cloudflared_image=${{ vars.AWS_CLOUDFLARED_IMAGE }}"
          -var="certificate_arn=${{ vars.AWS_PRIVATE_ALB_CERTIFICATE_ARN }}"
          -var="terraform_state_bucket_arn=arn:aws:s3:::${{ vars.AWS_TERRAFORM_STATE_BUCKET }}"
          -var="expected_aws_account_id=${{ vars.AWS_PRODUCTION_ACCOUNT_ID }}"
          -var="backup_destination_vault_arn=${{ vars.AWS_BACKUP_DESTINATION_VAULT_ARN }}"
          -var="play_integrity_cloud_project_number=${{ vars.PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER }}"
          -var="monthly_budget_usd=${{ vars.AWS_PRODUCTION_MONTHLY_BUDGET_USD }}"
          -var="budget_email=${{ vars.AWS_BUDGET_EMAIL }}"
          -var="secondary_budget_email=${{ vars.AWS_SECONDARY_BUDGET_EMAIL }}"
          -var="github_oidc_provider_arn=${{ vars.AWS_GITHUB_OIDC_PROVIDER_ARN }}"
          -var="services_enabled=true"
      - name: Record enrichment deployment evidence
        run: |
          bucket="$(terraform -chdir=infra/terraform/production output -raw receipt_bucket_name)"
          backup_vault="$(terraform -chdir=infra/terraform/production output -raw backup_vault_arn)"
          jq -n --arg commit "$GITHUB_SHA" --arg image "${{ steps.image.outputs.uri }}" --arg region "ap-south-1" --arg migration "0006_local_pilot.sql" --arg bucket "$bucket" --arg backup_vault "$backup_vault" '{commit_sha:$commit,image:$image,region:$region,migration:$migration,receipt_bucket:$bucket,backup_vault:$backup_vault,origin_exposure:"cloudflare-tunnel-private-alb",providers:{ocr:"textract",gst:"disabled",notifications:"disabled",credit:"disabled",slack:"disabled"}}' > enrichment-deployment.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: challanse-enrichment-deployment
          path: enrichment-deployment.json
          if-no-files-found: error

  deploy-landing:
    # GitHub Pages deployment of the INFORMATIONAL landing site
    # (challanse.constrovet.com). This job only builds the static landing,
    # configures Pages, uploads dist/landing, and deploys Pages. It has no
    # application/API dependency, no invoice upload, and no secret dependency.
    # It intentionally does NOT depend on PILOT_DEPLOY_ENABLED so the
    # informational landing can be published from protected green main even
    # while the pilot flag is false.
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: [validate, android, enrichment, security]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: production
      url: https://challanse.constrovet.com
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build:landing
        env:
          CHALLANSE_PILOT_REQUESTS_ENABLED: "false"
          CHALLANSE_CONTACT_URL: https://www.constrovet.com/pages/contact.html?interest=challanse
          CHALLANSE_CUSTOM_DOMAIN: challanse.constrovet.com
      - name: Copy CNAME into Pages artifact
        run: cp CNAME dist/landing/CNAME
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: dist/landing
      - id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5

  deploy-cloudflare:
    # Cloudflare-native deployment: Workers (API + Reviewer), D1 migrations, Queues.
    # Runs independently of the AWS enrichment deployment path.
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && vars.PILOT_DEPLOY_ENABLED == 'true'
    needs: [validate, android, enrichment, security]
    runs-on: ubuntu-latest
    environment:
      name: production
    outputs:
      api_worker_version: ${{ steps.worker_versions.outputs.api }}
      reviewer_worker_version: ${{ steps.worker_versions.outputs.reviewer }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci

      # Bootstrap Cloudflare resources (idempotent — safe to run on every deploy)
      - name: Create or verify Cloudflare resources
        run: bash scripts/setup-cloudflare.sh
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Apply D1 database migrations
      - name: Apply D1 database migrations
        run: npx wrangler d1 migrations apply challanse-core --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Render wrangler configuration with all resource IDs from the bootstrap step
      - name: Generate Worker configuration
        run: |
          # Source the resource IDs created by setup-cloudflare.sh
          set -a; source .cloudflare/resources.env; set +a
          node scripts/render-edge-config.mjs --production
        env:
          CLOUDFLARE_ACCESS_TEAM_DOMAIN: ${{ vars.CLOUDFLARE_ACCESS_TEAM_DOMAIN }}
          CLOUDFLARE_ACCESS_AUD: ${{ vars.CLOUDFLARE_ACCESS_AUD }}
          ENRICHMENT_URL: ${{ vars.ENRICHMENT_URL }}
          EDGE_TO_ENRICHMENT_HMAC_KEY_ID: ${{ vars.EDGE_TO_ENRICHMENT_HMAC_KEY_ID }}
          EDGE_TO_ENRICHMENT_NEXT_HMAC_KEY_ID: ${{ vars.EDGE_TO_ENRICHMENT_NEXT_HMAC_KEY_ID }}
          TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER: ${{ vars.PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER }}
          GROK_API_BASE_URL: ${{ vars.GROK_API_BASE_URL || 'https://api.x.ai/v1' }}
          AGENTMEMORY_URL: ${{ vars.AGENTMEMORY_URL }}

      # Configure API Worker secrets
      - name: Configure Worker secrets — Access & Turnstile
        run: |
          printf '%s' "$TURNSTILE_SECRET" | npx wrangler secret put TURNSTILE_SECRET --config apps/edge/config/edge.production.toml
          printf '%s' "$DEVICE_TOKEN_PEPPER" | npx wrangler secret put DEVICE_TOKEN_PEPPER --config apps/edge/config/edge.production.toml
          printf '%s' "$SESSION_ENCRYPTION_KEY" | npx wrangler secret put SESSION_ENCRYPTION_KEY --config apps/edge/config/edge.production.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          TURNSTILE_SECRET: ${{ secrets.TURNSTILE_SECRET }}
          DEVICE_TOKEN_PEPPER: ${{ secrets.DEVICE_TOKEN_PEPPER }}
          SESSION_ENCRYPTION_KEY: ${{ secrets.SESSION_ENCRYPTION_KEY }}

      - name: Configure Worker secrets — HMAC & enrichment proxy
        run: |
          printf '%s' "$EDGE_TO_ENRICHMENT_HMAC_KEY" | npx wrangler secret put EDGE_TO_ENRICHMENT_HMAC_KEY --config apps/edge/config/edge.production.toml
          printf '%s' "$EDGE_TO_ENRICHMENT_NEXT_HMAC_KEY" | npx wrangler secret put EDGE_TO_ENRICHMENT_NEXT_HMAC_KEY --config apps/edge/config/edge.production.toml
          printf '%s' "$TENANT_CONTEXT_HMAC_KEY" | npx wrangler secret put TENANT_CONTEXT_HMAC_KEY --config apps/edge/config/edge.production.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          EDGE_TO_ENRICHMENT_HMAC_KEY: ${{ secrets.EDGE_TO_ENRICHMENT_HMAC_KEY }}
          EDGE_TO_ENRICHMENT_NEXT_HMAC_KEY: ${{ secrets.EDGE_TO_ENRICHMENT_NEXT_HMAC_KEY }}
          TENANT_CONTEXT_HMAC_KEY: ${{ secrets.TENANT_CONTEXT_HMAC_KEY }}

      - name: Configure Worker secrets — xAI Grok & agentmemory
        run: |
          printf '%s' "$XAI_API_KEY" | npx wrangler secret put XAI_API_KEY --config apps/edge/config/edge.production.toml
          printf '%s' "$AGENTMEMORY_API_KEY" | npx wrangler secret put AGENTMEMORY_API_KEY --config apps/edge/config/edge.production.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          AGENTMEMORY_API_KEY: ${{ secrets.AGENTMEMORY_API_KEY }}

      - name: Configure Worker secrets — Play Integrity
        run: |
          printf '%s' "$PLAY_INTEGRITY_CREDENTIALS_JSON" | npx wrangler secret put PLAY_INTEGRITY_CREDENTIALS_JSON --config apps/edge/config/edge.production.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PLAY_INTEGRITY_CREDENTIALS_JSON: ${{ secrets.PLAY_INTEGRITY_CREDENTIALS_JSON }}

      # Deploy API Worker (now the full backend, not just a proxy)
      - name: Deploy API Worker
        run: npx wrangler deploy --config apps/edge/config/edge.production.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Deploy Queue consumers (placeholder — consumer Workers built in Phase 1)
      # Phase 1 will add `wrangler deploy --config apps/queue-consumer/wrangler.toml`

      # Build and deploy the Reviewer SPA
      - name: Build reviewer
        run: npm run build --workspace @challanse/reviewer
        env:
          VITE_API_BASE_URL: /api
          VITE_PUBLIC_API_URL: https://api.challanse.constrovet.com
      - name: Deploy reviewer
        run: npx wrangler deploy --config apps/reviewer/wrangler.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Record deployed Worker versions
      - name: Record deployed Worker versions
        id: worker_versions
        run: |
          api="$(curl --fail --silent --show-error -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/workers/scripts/challanse-api/versions" | jq -er '.result[0].id')"
          reviewer="$(curl --fail --silent --show-error -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/workers/scripts/challanse-reviewer/versions" | jq -er '.result[0].id')"
          echo "api=$api" >> "$GITHUB_OUTPUT"
          echo "reviewer=$reviewer" >> "$GITHUB_OUTPUT"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  release-android:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && vars.PILOT_DEPLOY_ENABLED == 'true' && vars.PLAY_PUBLISH_ENABLED == 'true'
    needs: [deploy-cloudflare, deploy-landing]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    environment:
      name: production
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5
        with:
          distribution: temurin
          java-version: 17
          cache: gradle
      - run: npm ci
      - name: Require production HTTPS and fail-closed provider state
        run: |
          test "$(curl --fail --silent --show-error -o /dev/null -w '%{http_code}' https://challanse.constrovet.com/)" = "200"
          health="$(curl --fail --silent --show-error https://api.challanse.constrovet.com/health)"
          ready="$(curl --fail --silent --show-error https://api.challanse.constrovet.com/ready)"
          jq -e '.status == "ok"' >/dev/null <<<"$health"
          jq -e '.status == "ready"' >/dev/null <<<"$ready"
          reviewer="$(curl --silent --show-error -o /dev/null -w '%{http_code}' https://review.challanse.constrovet.com/)"
          [[ "$reviewer" == "302" || "$reviewer" == "403" ]]
      - name: Restore release keystore
        run: echo "$CHALLANSE_KEYSTORE_BASE64" | base64 --decode > apps/mobile/android/challanse-release.jks
        env:
          CHALLANSE_KEYSTORE_BASE64: ${{ secrets.CHALLANSE_KEYSTORE_BASE64 }}
      - run: npm run build:release --workspace @challanse/mobile
        env:
          CHALLANSE_KEYSTORE_FILE: ../challanse-release.jks
          CHALLANSE_KEYSTORE_PASSWORD: ${{ secrets.CHALLANSE_KEYSTORE_PASSWORD }}
          CHALLANSE_KEY_ALIAS: ${{ secrets.CHALLANSE_KEY_ALIAS }}
          CHALLANSE_KEY_PASSWORD: ${{ secrets.CHALLANSE_KEY_PASSWORD }}
          CHALLANSE_VERSION_CODE: ${{ github.run_number }}
          CHALLANSE_VERSION_NAME: 1.0.${{ github.run_number }}
          PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER: ${{ vars.PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER }}
      - name: Remove restored upload keystore
        if: always()
        run: rm -f apps/mobile/android/challanse-release.jks
      - name: Verify signing identity and create release manifest
        run: |
          bundle="apps/mobile/android/app/build/outputs/bundle/release/app-release.aab"
          fingerprint="$(keytool -printcert -jarfile "$bundle" | sed -n 's/^[[:space:]]*SHA256:[[:space:]]*//p' | head -1 | tr -d ':' | tr '[:lower:]' '[:upper:]')"
          test "$fingerprint" = "$EXPECTED_UPLOAD_FINGERPRINT"
          test -n "$PLAY_APP_SIGNING_FINGERPRINT"
          test -z "$REVOKED_FINGERPRINT" || test "$fingerprint" != "$REVOKED_FINGERPRINT"
          [[ "$PLAY_RELEASE_TRACK" =~ ^(internal|alpha|production)$ ]]
          [[ "$PLAY_MANAGED_ORGANIZATIONS_SHA256" =~ ^[a-f0-9]{64}$ ]]
          test "$PLAY_MANAGED_ORGANIZATIONS_COUNT" -ge 1
          aab_sha256="$(sha256sum "$bundle" | awk '{print $1}')"
          npm sbom --sbom-format cyclonedx > mobile-sbom.cdx.json
          sbom_sha256="$(sha256sum mobile-sbom.cdx.json | awk '{print $1}')"
          jq -n --arg commit "$GITHUB_SHA" --arg run_id "$GITHUB_RUN_ID" --arg aab_sha256 "$aab_sha256" \
            --arg upload_cert_sha256 "$fingerprint" --arg play_app_signing_cert_sha256 "$PLAY_APP_SIGNING_FINGERPRINT" \
            --arg revoked_signing_cert_sha256 "$REVOKED_FINGERPRINT" --arg sbom_sha256 "$sbom_sha256" \
            --arg play_track "$PLAY_RELEASE_TRACK" --arg managed_organizations_sha256 "$PLAY_MANAGED_ORGANIZATIONS_SHA256" \
            --arg managed_organizations_count "$PLAY_MANAGED_ORGANIZATIONS_COUNT" \
            --arg api_worker_version "$API_WORKER_VERSION" --arg reviewer_worker_version "$REVIEWER_WORKER_VERSION" \
            --arg staging_acceptance_sha256 "$STAGING_ACCEPTANCE_SHA256" --arg android_field_acceptance_sha256 "$ANDROID_FIELD_ACCEPTANCE_SHA256" \
            --arg client_acceptance_sha256 "$CLIENT_ACCEPTANCE_SHA256" --arg operator_training_sha256 "$OPERATOR_TRAINING_SHA256" \
            --arg security_acceptance_sha256 "$SECURITY_ACCEPTANCE_SHA256" --arg capacity_acceptance_sha256 "$CAPACITY_ACCEPTANCE_SHA256" \
            --arg recovery_acceptance_sha256 "$RECOVERY_ACCEPTANCE_SHA256" \
            --arg version_code "$GITHUB_RUN_NUMBER" --arg version_name "1.0.$GITHUB_RUN_NUMBER" \
            '{schema_version:"2.3",distribution:"managed-google-play-private",play_track:$play_track,managed_organizations:{count:($managed_organizations_count|tonumber),sha256:$managed_organizations_sha256},version_code:($version_code|tonumber),version_name:$version_name,commit_sha:$commit,workflow_run_id:$run_id,aab_sha256:$aab_sha256,upload_certificate_sha256:$upload_cert_sha256,play_app_signing_certificate_sha256:$play_app_signing_cert_sha256,revoked_signing_certificate_sha256:$revoked_signing_cert_sha256,sbom_sha256:$sbom_sha256,cloudflare:{api_worker_version:$api_worker_version,reviewer_worker_version:$reviewer_worker_version},acceptance:{staging_sha256:$staging_acceptance_sha256,android_field_sha256:$android_field_acceptance_sha256,client_sha256:$client_acceptance_sha256,operator_training_sha256:$operator_training_sha256,security_sha256:$security_acceptance_sha256,capacity_sha256:$capacity_acceptance_sha256,recovery_sha256:$recovery_acceptance_sha256},verification:{https:true,health:true,readiness:true,access_gate:true},external_providers:{ocr:"grok-vision",gst:"aafi-integration",notifications:"disabled",credit:"disabled",slack:"disabled"}}' > release-manifest.json
        env:
          EXPECTED_UPLOAD_FINGERPRINT: ${{ vars.CHALLANSE_UPLOAD_CERT_SHA256 }}
          PLAY_APP_SIGNING_FINGERPRINT: ${{ vars.CHALLANSE_PLAY_APP_SIGNING_CERT_SHA256 }}
          REVOKED_FINGERPRINT: ${{ vars.CHALLANSE_REVOKED_SIGNING_CERT_SHA256 }}
          API_WORKER_VERSION: ${{ needs.deploy-cloudflare.outputs.api_worker_version }}
          REVIEWER_WORKER_VERSION: ${{ needs.deploy-cloudflare.outputs.reviewer_worker_version }}
          STAGING_ACCEPTANCE_SHA256: ${{ vars.STAGING_ACCEPTANCE_SHA256 }}
          ANDROID_FIELD_ACCEPTANCE_SHA256: ${{ vars.ANDROID_FIELD_ACCEPTANCE_SHA256 }}
          CLIENT_ACCEPTANCE_SHA256: ${{ vars.CLIENT_ACCEPTANCE_SHA256 }}
          OPERATOR_TRAINING_SHA256: ${{ vars.OPERATOR_TRAINING_SHA256 }}
          SECURITY_ACCEPTANCE_SHA256: ${{ vars.SECURITY_ACCEPTANCE_SHA256 }}
          CAPACITY_ACCEPTANCE_SHA256: ${{ vars.CAPACITY_ACCEPTANCE_SHA256 }}
          RECOVERY_ACCEPTANCE_SHA256: ${{ vars.RECOVERY_ACCEPTANCE_SHA256 }}
          PLAY_RELEASE_TRACK: ${{ vars.PLAY_RELEASE_TRACK }}
          PLAY_MANAGED_ORGANIZATIONS_SHA256: ${{ vars.PLAY_MANAGED_ORGANIZATIONS_SHA256 }}
          PLAY_MANAGED_ORGANIZATIONS_COUNT: ${{ vars.PLAY_MANAGED_ORGANIZATIONS_COUNT }}
      - uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3
        with:
          subject-path: apps/mobile/android/app/build/outputs/bundle/release/app-release.aab
      - name: Publish Managed Google Play private track
        uses: r0adkll/upload-google-play@935ef9c68bb393a8e6116b1575626a7f5be3a7fb # v1.1.3
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.constrovet.challanse
          releaseFiles: apps/mobile/android/app/build/outputs/bundle/release/app-release.aab
          track: ${{ vars.PLAY_RELEASE_TRACK }}
          status: completed
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: challanse-android-app-bundle
          path: apps/mobile/android/app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: challanse-release-manifest
          path: release-manifest.json
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: challanse-mobile-sbom
          path: mobile-sbom.cdx.json
          if-no-files-found: error
