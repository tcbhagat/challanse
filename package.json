{
  "name": "challanse",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "npm run check:html && npm run check:js && npm run check --workspaces --if-present",
    "check:html": "html-validate index.html",
    "check:js": "node --check assets/js/main.js && node --check assets/js/challanse.js",
    "test": "npm run test --workspaces --if-present && node --test tests/security/*.test.mjs",
    "test:browser": "playwright test",
    "test:landing": "node --test tests/landing/build-output.test.mjs",
    "test:landing:browser": "playwright test --config playwright.landing.config.ts",
    "test:enrichment": "PYTHONPATH=services/enrichment python3 -m pytest -q services/enrichment/tests",
    "build": "npm run build:landing && npm run build --workspace @challanse/contracts --if-present && npm run build --workspace @challanse/edge && npm run build --workspace @challanse/reviewer",
    "build:landing": "node scripts/build-landing.mjs",
    "audit": "for run in 1 2 3; do lighthouse http://127.0.0.1:4173/ --only-categories=performance,accessibility,best-practices,seo --output=json --output-path=.lighthouse-$run.json --quiet --chrome-flags=\"--headless=new --no-sandbox --disable-gpu\" || exit 1; done && node scripts/check-lighthouse.mjs .lighthouse-1.json .lighthouse-2.json .lighthouse-3.json",
    "audit:production": "node scripts/check-npm-audit.mjs",
    "cf:setup": "bash scripts/setup-cloudflare.sh",
    "cf:setup:preview": "bash scripts/setup-cloudflare.sh --preview",
    "cf:migrate": "npx wrangler d1 migrations apply challanse-core",
    "cf:migrate:local": "npx wrangler d1 migrations apply challanse-core --local",
    "cf:deploy": "node scripts/render-edge-config.mjs --production && npx wrangler deploy --config apps/edge/config/edge.production.toml",
    "cf:deploy:reviewer": "npx wrangler deploy --config apps/reviewer/wrangler.toml",
    "cf:dev": "npx wrangler dev --config apps/edge/wrangler.toml --local",
    "cf:secrets": "node scripts/render-edge-config.mjs --production",
    "db:seed": "node scripts/seed-d1.mjs"
  },
  "overrides": {
    "@istanbuljs/load-nyc-config": {
      "js-yaml": "3.15.1"
    },
    "@typescript-eslint/typescript-estree": {
      "minimatch": "10.2.6"
    },
    "brace-expansion": "5.0.9",
    "js-yaml": "4.3.1",
    "undici": "7.29.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "html-validate": "11.5.6",
    "lighthouse": "13.4.1",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  }
}
